<!DOCTYPE html>
<html>
    <head>
        {% block head %}
            <link href="http://flintholbrook.com/webroot/styles/styles.css" rel="stylesheet">
            <title>{% block title %}{% endblock %}</title>
            <style type="text/css">
                body { padding: 10px; }
            </style>
        {% endblock %}
    </head>
    <body>
          <div class="wrapper">
            <div class="container">
                <header class="page-header">
                     <p style="float:right;"><a href="/user/logout">Logout</a></p>
                    <h1>Flint's Twitter Clone</h1>
                    <p>It is built on Silex and uses mysql as the database.</p>
                </header>
            </div>
            <div class="container">
                
                <div class="col-md-12">
                    {% if app.session.flashbag.has('alert') %}
                        {% for flashMessage in app.session.flashbag.get('alert') %}
                            <div class="alert">{{ flashMessage }}</div>
                        {% endfor %}
                    {% endif %}
                    <div id="content">{% block content %}{% endblock %}</div>
                </div>
            </div>
          </div>
    </body>
</html>